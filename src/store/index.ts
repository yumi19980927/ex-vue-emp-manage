import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  strict: true,
  state: {}, // end state
  actions: {}, // end actions
  mutations: {}, // end mutations
  getters: {}, // end getters
  modules: {}, // end modules
});
